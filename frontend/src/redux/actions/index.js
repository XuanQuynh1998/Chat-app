import { showProfile, showSettings, editProfile } from "./rightbarAction.js";
import { loginAction, authenticateAction } from "./users.js";

const allActions = {
  showProfile,
  showSettings,
  editProfile,
  loginAction,
  authenticateAction,
};

export default allActions;
